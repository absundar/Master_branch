package abstraction;

public interface UpcomingProjects extends Interface{ 

	String upcomingProjects= "New Music system";  //by default interface variables are public, static and final
	
	void employee();  //by default interface method will be public abstract so no body
	//no objects cannot be created as there wont be any implementation
}
